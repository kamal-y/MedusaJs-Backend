// import productEventHandler, {
//   config as productEventConfig,
// } from "../subscribers/directus-sync-subscriber";

// export default async ({ container }: { container: any }): Promise<void> => {
//   const eventBusService = container.resolve("eventBusService");

//   // Register the product event subscriber
//   eventBusService.subscribe(productEventConfig.event, productEventHandler);
// };
